# Validation Artifacts

This directory stores generated solver and validation outputs.

Typical contents:

- CP-SAT mapping/scheduling results
- QUBO model exports
- QUBO energy test reports
- brute-force comparison reports
- assignment validator outputs
- dataset metadata summary JSON

These files are generated by scripts in `src/` and are safe to regenerate from the raw trace and the example windows.
